<?xml version="1.0" encoding="utf-8"?>
<network-security-config>
    <!-- Profile builds override the main network security config (profile res
         wins over main res at build time). Allow cleartext to any host so
         development against a LAN backend (physical phone) just works. -->
    <base-config cleartextTrafficPermitted="true" />
</network-security-config>