<?xml version="1.0" encoding="utf-8"?>
<network-security-config>
    <!-- The dev backend runs over plain HTTP on the host machine. Allow
         cleartext only for the emulator host loopback and localhost;
         all other traffic must use HTTPS. -->
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">10.0.2.2</domain>
        <domain includeSubdomains="false">localhost</domain>
        <domain includeSubdomains="false">127.0.0.1</domain>
        <domain includeSubdomains="false">192.168.1.4</domain>
    </domain-config>
</network-security-config>